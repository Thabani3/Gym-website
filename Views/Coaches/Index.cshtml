@model IEnumerable<HealthAW.Models.Coach>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CoachTypeNames)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CoachExperience)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CoachTypeNames)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CoachExperience)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CoachId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CoachId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CoachId })
        </td>
    </tr>
}

</table>
